/**
 * @typedef {Object} AgentBlueprint
 * @property {string} role - The role of the agent (e.g., CEO, CFO, CTO)
 * @property {string[]} tools - Array of tool names that the agent can use
 * @property {('short-term'|'long-term')} [memory] - Type of memory for the agent
 * @property {string} [llm] - The language model to use (e.g., "gpt-4-turbo")
 */

module.exports = {}; // Just exporting the JSDoc type definition